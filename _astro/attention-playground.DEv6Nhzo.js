import{w as S,t as j}from"./Base.astro_astro_type_script_index_0_lang.65xM_WgJ.js";function E(d){const x=S(d,"Attention, computed before your eyes");x.innerHTML=`
    <div class="w-row">
      <label><input type="checkbox" id="att-scale" checked /> divide by √d_k</label>
      <label><input type="checkbox" id="att-causal" /> causal mask</label>
      <span class="w-out">d_k = 4</span>
    </div>
    <div id="att-grid" style="overflow-x:auto"></div>
    <div class="w-row" style="font-size:0.78rem;color:var(--ink-faint)">
      Use the +/− controls to nudge any raw score q·k. Cell shading shows the softmax weight; each row of weights sums to 1, and each output row is that weighted mix of the value vectors. Un-check “divide by √d_k” to watch softmax saturate toward one-hot — the very problem the scaling prevents. Turn on the causal mask to see future positions get −∞.
    </div>
  `;const m=["The","robot","lifted","it"],l=[[2,.4,.1,.2],[.8,2.2,.5,.3],[.3,1.6,2.1,.4],[.2,3.1,1.2,1.8]].map(i=>[...i]),k=[[1,.1],[.2,1.2],[-.8,.6],[.4,-.9]],v=4,y=d.querySelector("#att-grid"),b=d.querySelector("#att-scale"),f=d.querySelector("#att-causal");function w(i){const u=Math.max(...i.filter(a=>a!==-1/0)),h=i.map(a=>a===-1/0?0:Math.exp(a-u)),c=h.reduce((a,o)=>a+o,0);return h.map(a=>c>0?a/c:0)}function p(){j();const i=b.checked?Math.sqrt(v):1,u=f.checked,c=l.map((t,s)=>t.map((e,n)=>u&&n>s?-1/0:e/i)).map(w),a=c.map(t=>[0,1].map(s=>t.reduce((e,n,g)=>e+n*k[g][s],0))),o=(t,s="",e="")=>`<td style="border:1px solid var(--line);padding:0.28rem 0.45rem;text-align:center;font-family:var(--font-mono);font-size:0.72rem;${s};${e}">${t}</td>`;let r='<table style="border-collapse:collapse;margin:0.4rem 0"><tr><td></td>';r+=m.map(t=>o(`<strong>${t}</strong><br/><span style="opacity:0.6">key</span>`)).join(""),r+=o("")+o("<strong>weights → output</strong>","","text-align:left"),r+="</tr>";for(let t=0;t<4;t++){r+=`<tr>${o(`<strong>${m[t]}</strong><br/><span style="opacity:0.6">query</span>`)}`;for(let e=0;e<4;e++){const n=u&&e>t,g=n?"−∞":l[t][e].toFixed(1),$=c[t][e],M=n?"background:repeating-linear-gradient(45deg,transparent,transparent 3px,var(--line) 3px,var(--line) 4px)":`background:rgba(20,184,166,${($*.85).toFixed(3)})`,q=n?"":`<br/><span data-i="${t}" data-j="${e}" data-d="-1" class="att-btn" style="cursor:pointer;padding:0 0.25rem;user-select:none">−</span><span data-i="${t}" data-j="${e}" data-d="1" class="att-btn" style="cursor:pointer;padding:0 0.25rem;user-select:none">+</span>`;r+=o(`${g}${q}`,M)}const s=c[t].map((e,n)=>`<span title="${m[n]}" style="display:inline-block;width:${Math.max(2,e*68)}px;height:9px;background:#14b8a6;opacity:${.35+e*.65};margin-right:2px;border-radius:2px"></span>`).join("");r+=o("=")+o(`${s}<br/>out = [${a[t][0].toFixed(2)}, ${a[t][1].toFixed(2)}]`,"","text-align:left;min-width:9.5rem"),r+="</tr>"}r+="</table>",y.innerHTML=r,y.querySelectorAll(".att-btn").forEach(t=>t.addEventListener("click",()=>{const{i:s,j:e,d:n}=t.dataset;l[+s][+e]=Math.max(-4,Math.min(6,l[+s][+e]+.5*+n)),p()}))}b.addEventListener("change",p),f.addEventListener("change",p),p()}export{E as default};
